{
  "config": {
    "step": {
      "user": {
        "description": "Please add the Smappee Platform API key",
        "data": {
          "client_id": "Your API client id",
          "client_secret": "Your client secret",
          "username": "Your username on Smappee dashboard",
          "password": "Your password on Smappee dashboard",
          "serial": "The serial number of your charging point"
        }
      }
    },
    "error": {
      "auth_failed": "Authorization failed"
    }
  },
  "options": {
    "step": {
      "init": {
        "description": "Update your credentials and polling interval",
        "data": {
          "client_id": "Your API client id",
          "client_secret": "Your client secret",
          "username": "Your username on Smappee dashboard",
          "password": "Your password on Smappee dashboard",
          "serial": "The serial number of your charging point"
        }
      }
    },
    "error": {
      "already_configured": "An instance with this serial already exists"
    }
  },
  "services": {
    "start_charging": {
      "name": "Start charging",
      "description": "Start charging. If 'current' is omitted, the configured or minimum current will be used.",
      "fields": {
        "config_entry_id": {
          "name": "Config entry ID",
          "description": "Optional: specific config entry id to target (when multiple entries)."
        },
        "service_location_id": {
          "name": "Service Location ID",
          "description": "Optional: ID of the site (leave empty if only one site)."
        },
        "connector_id": {
          "name": "Connector ID",
          "description": "Optional: connector number (1 or 2, leave empty if only one)."
        },
        "current": {
          "name": "Current (A)",
          "description": "Optional: charging current to use (in Ampere)."
        }
      }
    },
    "pause_charging": {
      "name": "Pause charging",
      "description": "Pause charging for the selected connector.",
      "fields": {
        "config_entry_id": {
          "name": "Config entry ID",
          "description": "Optional: specific config entry id to target (when multiple entries)."
        },
        "service_location_id": {
          "name": "Service Location ID",
          "description": "Optional: ID of the site (leave empty if only one site)."
        },
        "connector_id": {
          "name": "Connector ID",
          "description": "Optional: connector number (1 or 2)."
        }
      }
    },
    "stop_charging": {
      "name": "Stop charging",
      "description": "Stop charging for the selected connector.",
      "fields": {
        "config_entry_id": {
          "name": "Config entry ID",
          "description": "Optional: specific config entry id to target (when multiple entries)."
        },
        "service_location_id": {
          "name": "Service Location ID",
          "description": "Optional: ID of the site (leave empty if only one site)."
        },
        "connector_id": {
          "name": "Connector ID",
          "description": "Optional: connector number (1 or 2)."
        }
      }
    },
    "set_charging_mode": {
      "name": "Set charging mode",
      "description": "Set the charging mode of a connector.",
      "fields": {
        "config_entry_id": {
          "name": "Config entry ID",
          "description": "Optional: specific config entry id to target (when multiple entries)."
        },
        "service_location_id": {
          "name": "Service Location ID",
          "description": "Optional: ID of the site (leave empty if only one site)."
        },
        "connector_id": {
          "name": "Connector ID",
          "description": "Optional: connector number (1 or 2)."
        },
        "mode": {
          "name": "Mode",
          "description": "Charging mode (NORMAL, SMART, SOLAR)."
        }
      }
    },
    "set_charging_mode_api2": {
      "name": "Set charging mode (legacy)",
      "description": "Set the charging mode using the legacy chargingstations endpoint.",
      "fields": {
        "config_entry_id": {
          "name": "Config entry ID",
          "description": "Optional: specific config entry id to target (when multiple entries)."
        },
        "service_location_id": {
          "name": "Service Location ID",
          "description": "Optional: ID of the site (leave empty if only one site)."
        },
        "connector_id": {
          "name": "Connector ID",
          "description": "Optional: connector number (1 or 2)."
        },
        "mode": {
          "name": "Mode",
          "description": "Charging mode (NORMAL, SMART, PAUSED)."
        },
        "limit": {
          "name": "Limit",
          "description": "Optional: limit value (only used when mode is NORMAL)."
        },
        "limit_unit": {
          "name": "Limit unit",
          "description": "Optional: unit for limit when mode is NORMAL (AMPERE or PERCENTAGE)."
        }
      }
    }
  }
}