{
  "config": {
    "step": {
      "user": {
        "description": "Please add the Smappee Platform API key",
        "data": {
          "client_id": "Your API client id",
          "client_secret": "Your client secret",
          "username": "Your username on Smappee dashboard",
          "password": "Your password on Smappee dashboard"
        }
      },
      "reauth_confirm": {
        "description": "Update your Smappee API credentials to reconnect",
        "data": {
          "client_id": "Your API client id",
          "client_secret": "Your client secret",
          "username": "Your username on Smappee dashboard",
          "password": "Your password on Smappee dashboard"
        }
      },
      "reconfigure": {
        "description": "Update your Smappee API credentials",
        "data": {
          "client_id": "Your API client id",
          "client_secret": "Your client secret",
          "username": "Your username on Smappee dashboard",
          "password": "Your password on Smappee dashboard"
        }
      }
    },
    "error": {
      "auth_failed": "Authorization failed"
    }
  },
  "services": {
    "start_charging": {
      "name": "Start charging",
      "description": "Start charging. If 'current' is omitted, the configured or minimum current will be used.",
      "fields": {
        "config_entry_id": {
          "name": "Config entry ID",
          "description": "Optional: specific config entry id to target (when multiple entries)."
        },
        "service_location_id": {
          "name": "Service Location ID",
          "description": "Optional: ID of the site (leave empty if only one site)."
        },
        "connector_id": {
          "name": "Connector ID",
          "description": "Optional: connector number (1 or 2, leave empty if only one)."
        },
        "current": {
          "name": "Current (A)",
          "description": "Optional: charging current to use (in Ampere)."
        }
      }
    },
    "pause_charging": {
      "name": "Pause charging",
      "description": "Pause charging for the selected connector. Uses the smartdevices API endpoint (recommended \u2014 matches Smappee app behaviour).",
      "fields": {
        "config_entry_id": {
          "name": "Config entry ID",
          "description": "Optional: specific config entry id to target (when multiple entries)."
        },
        "service_location_id": {
          "name": "Service Location ID",
          "description": "Optional: ID of the site (leave empty if only one site)."
        },
        "connector_id": {
          "name": "Connector ID",
          "description": "Optional: connector number (1 or 2)."
        }
      }
    },
    "pause_charging_chargingstations": {
      "name": "Pause charging (chargingstations)",
      "description": "Pause charging via the chargingstations API endpoint. Legacy fallback \u2014 use pause_charging for normal operation.",
      "fields": {
        "config_entry_id": {
          "name": "Config entry ID",
          "description": "Optional: specific config entry id to target (when multiple entries)."
        },
        "service_location_id": {
          "name": "Service Location ID",
          "description": "Optional: ID of the site (leave empty if only one site)."
        },
        "connector_id": {
          "name": "Connector ID",
          "description": "Optional: connector number (1 or 2)."
        }
      }
    },
    "stop_charging": {
      "name": "Stop charging",
      "description": "Stop charging for the selected connector.",
      "fields": {
        "config_entry_id": {
          "name": "Config entry ID",
          "description": "Optional: specific config entry id to target (when multiple entries)."
        },
        "service_location_id": {
          "name": "Service Location ID",
          "description": "Optional: ID of the site (leave empty if only one site)."
        },
        "connector_id": {
          "name": "Connector ID",
          "description": "Optional: connector number (1 or 2)."
        }
      }
    },
    "set_current": {
      "name": "Set current",
      "description": "Set the charging current in Ampere (1 decimal precision). Converted to the nearest integer percentage and sent via setPercentageLimit. The slider and number entities update immediately.",
      "fields": {
        "config_entry_id": {
          "name": "Config entry ID",
          "description": "Optional: specific config entry id to target (when multiple entries)."
        },
        "service_location_id": {
          "name": "Service Location ID",
          "description": "Optional: ID of the site (leave empty if only one site)."
        },
        "connector_id": {
          "name": "Connector ID",
          "description": "Optional: connector number (1 or 2, leave empty if only one)."
        },
        "current": {
          "name": "Current (A)",
          "description": "Charging current in Ampere (1 decimal, e.g. 11.4). Clamped to the connector's configured min\u2013max range."
        }
      }
    },
    "set_charging_mode": {
      "name": "Set charging mode",
      "description": "Set the charging mode of a connector.",
      "fields": {
        "config_entry_id": {
          "name": "Config entry ID",
          "description": "Optional: specific config entry id to target (when multiple entries)."
        },
        "service_location_id": {
          "name": "Service Location ID",
          "description": "Optional: ID of the site (leave empty if only one site)."
        },
        "connector_id": {
          "name": "Connector ID",
          "description": "Optional: connector number (1 or 2)."
        },
        "mode": {
          "name": "Mode",
          "description": "Charging mode (STANDARD, SMART, SOLAR)."
        }
      }
    },
    "set_charging_mode_chargingstations": {
      "name": "Set charging mode (chargingstations)",
      "description": "Set the charging mode using the chargingstations endpoint.",
      "fields": {
        "config_entry_id": {
          "name": "Config entry ID",
          "description": "Optional: specific config entry id to target (when multiple entries)."
        },
        "charging_station_serial": {
          "name": "Charging station serial",
          "description": "Optional: charging station serial number. Provide it when multiple stations share the same connector position."
        },
        "connector_id": {
          "name": "Connector ID",
          "description": "Required: connector position used by the chargingstations endpoint (for example 1 or 2)."
        },
        "mode": {
          "name": "Mode",
          "description": "Charging mode (NORMAL, SMART, PAUSED)."
        },
        "limit": {
          "name": "Limit",
          "description": "Optional: limit value (only used when mode is NORMAL)."
        },
        "limit_unit": {
          "name": "Limit unit",
          "description": "Optional: unit for limit when mode is NORMAL (AMPERE or PERCENTAGE)."
        }
      }
    }
  }
}
