[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
asyncio_default_fixture_loop_scope = function
# Keep pytest-socket enabled: HA installs its guard before each test and the
# plugin's teardown restores it. Disabling the plugin stacks guards on Linux.
